# Third-party notices

The package contains the MIT-licensed WABS plugin SDK and Zod, each with its original license in node_modules. Exact versions and integrity are recorded in package-lock.json; SDK provenance is in provenance.json.

The unmodified workspace protocol contract is MIT licensed. Its original bytes, source commit and checksum are retained in contracts/. It contains no workspace server implementation.
